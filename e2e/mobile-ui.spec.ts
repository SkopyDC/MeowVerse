import {test,expect,Page} from "@playwright/test";

const viewports=[{name:"iphone-se",width:320,height:568},{name:"iphone-15",width:393,height:852},{name:"iphone-17-pro-max",width:440,height:956}];
async function stable(page:Page){const size=await page.evaluate(()=>({scroll:document.documentElement.scrollWidth,client:document.documentElement.clientWidth,body:document.body.scrollWidth}));expect(size.scroll).toBeLessThanOrEqual(size.client);expect(size.body).toBeLessThanOrEqual(size.client);for(const button of await page.locator("button:visible").all()){const box=await button.boundingBox();expect(box?.width).toBeGreaterThan(0);expect(box?.x).toBeGreaterThanOrEqual(0);expect((box?.x||0)+(box?.width||0)).toBeLessThanOrEqual(size.client+1);}}
for(const viewport of viewports)test.describe(viewport.name,()=>{test.use({viewport});test("úvod a všechny hlavní kroky jsou responzivní",async({page})=>{await page.goto("/");await expect(page.getByText("Posbírej rybky")).toBeVisible();await stable(page);await page.getByRole("button",{name:/Rybník/}).click();await stable(page);await page.getByRole("button",{name:/Posbírat/}).click();await page.getByRole("button",{name:/Vylepšit/}).click();await expect(page.getByRole("button",{name:/Otevřít/})).toBeVisible();await stable(page);await page.getByRole("button",{name:/Otevřít/}).click();await page.getByRole("button",{name:"Rozhodnout o kočce"}).click();await stable(page);await page.getByRole("button",{name:/Do PvP/}).click();await stable(page);await page.locator("[data-fighter]").first().click();await page.getByRole("button",{name:"Bojovat"}).click();await expect(page.getByText(/Vítězství|Prohra/)).toBeVisible();await stable(page);});});
